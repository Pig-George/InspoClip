services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: inspoclip
      POSTGRES_PASSWORD: inspoclip
      POSTGRES_DB: inspoclip
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '127.0.0.1:5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U inspoclip']
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  server:
    build: ./server
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://inspoclip:inspoclip@postgres:5432/inspoclip
      AI_PROVIDER: ${AI_PROVIDER:-alibaba-bailian}
      AI_API_KEY: ${AI_API_KEY:-sk-placeholder}
      AI_API_BASE: ${AI_API_BASE:-https://dashscope.aliyuncs.com/compatible-mode/v1}
      AI_MODEL: ${AI_MODEL:-qwen3.7-plus}
      VIDEO_AI_PROVIDER: ${VIDEO_AI_PROVIDER:-alibaba-bailian}
      VIDEO_AI_API_KEY: ${VIDEO_AI_API_KEY:-}
      VIDEO_AI_API_BASE: ${VIDEO_AI_API_BASE:-https://dashscope.aliyuncs.com/compatible-mode/v1}
      VIDEO_AI_MODEL: ${VIDEO_AI_MODEL:-qwen3.7-plus}
      VIDEO_AI_FPS: ${VIDEO_AI_FPS:-3}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:8080}
      MODEL_VIDEO_PUBLIC_BASE_URL: ${MODEL_VIDEO_PUBLIC_BASE_URL:-http://localhost:8080}
      MODEL_VIDEO_VERIFY_BASE_URL: ${MODEL_VIDEO_VERIFY_BASE_URL:-}
      TUNNEL_MANAGER_URL: ${TUNNEL_MANAGER_URL:-http://tunnel-manager:3002}
      MODEL_VIDEO_ACCESS_TTL_MS: ${MODEL_VIDEO_ACCESS_TTL_MS:-7200000}
      PORT: 3001
      UPLOAD_DIR: /app/uploads
      VIDEO_UPLOAD_DIR: /app/videos
    volumes:
      - uploads:/app/uploads
      - videos:/app/videos
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - '127.0.0.1:3001:3001'
    healthcheck:
      test: ['CMD-SHELL', 'node -e "fetch(\"http://localhost:3001/api/health\").then(r => r.ok ? process.exit(0) : process.exit(1)).catch(() => process.exit(1))"']
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

  client:
    build: ./client
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - '${PORT:-8080}:80'

  tunnel-manager:
    build:
      context: .
      dockerfile: tunnel-manager/Dockerfile
    restart: unless-stopped
    environment:
      TUNNEL_TARGET_URL: ${TUNNEL_TARGET_URL:-http://client:80}
      TUNNEL_MANAGER_PORT: 3002
      TUNNEL_STARTUP_TIMEOUT_MS: ${TUNNEL_STARTUP_TIMEOUT_MS:-45000}
      TUNNEL_RESTART_DELAY_MS: ${TUNNEL_RESTART_DELAY_MS:-3000}
    depends_on:
      - client
    ports:
      - '127.0.0.1:3002:3002'

  video-public-proxy:
    image: nginx:alpine
    restart: unless-stopped
    depends_on:
      - server
    volumes:
      - ./video-public-proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  pgdata:
  uploads:
  videos:
